/**
 * The public homepage is rendered by the desktop OS shell in app/layout.tsx.
 * Keeping this route empty prevents a legacy page from being rendered behind it.
 */
export default function HomePage() {
  return null;
}
