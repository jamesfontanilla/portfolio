/**
 * /competitions is an OS deep-link. The layout opens the Competitions app
 * from this pathname; the collection is rendered inside that window.
 */
export default function CompetitionsPage() {
  return null;
}
