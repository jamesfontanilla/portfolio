/**
 * /projects is an OS deep-link. The layout opens the Projects app from this
 * pathname; the collection itself is rendered inside that window.
 */
export default function ProjectsPage() {
  return null;
}
